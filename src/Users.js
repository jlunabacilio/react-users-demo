import React, { useEffect } from 'react';
//import { Link } from 'react-router-dom';

function Users() {

    //const [users, setUsers] = React.useState([]);

    //here we fetch the data from the users API

    return (
        <div className="center-content">
            {/* I want to add a user mapping that allow me to display users id, fullname, and status from the API*/}

            {/* here add a button to go back to the home page*/}
        </div>
    );
}

export default Users;
